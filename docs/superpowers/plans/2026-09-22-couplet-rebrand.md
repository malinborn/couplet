# Ребрендинг mdmini → couplet

Хендофф-документ. Состояние на 2026-09-22, ветка `worktree-rebrand-couplet-preview`.

---

## Зачем

`mdmini.com` принадлежит чужому продукту (David Royal, «Tiny Local Productivity Apps»,
v0.1.1). Он забрал чистый `.com` и теперь выигрывает выдачу у `md-mini.com`.
Max был первым, но проиграл поиск. Аудитория ещё мала — момент для смены имени.

**Переименование — не косметика.** Имя зашито в каталог данных, сокет, CLI, MCP-сервер,
Homebrew-cask и в чужие CLAUDE.md. Дорогая часть — не код, а совместимость.

---

## Что решено

- **Полный разрыв** с `md`/`mini`. Не `mdX`, не `X-mini`.
- Имя должно нести **минимализм + ИИ**; слова «AI» в названии нет и не будет
  (так же, как у Cursor, Zed, Warp, Linear — иначе через два года снова переименовываться).
- Домен обязан быть **свободен и дёшев**. `.app`/`.dev` (~$9 / $14 продление) годятся,
  `.sh` ($31 / $47) — с натяжкой, бюджета на выкуп занятого домена нет.
- Max любит морской и технический нейминг в духе Kubernetes и Docker.
- **ИМЯ УТВЕРЖДЕНО ВЛАДЕЛЬЦЕМ: `couplet`, домен `couplet.pro`.**
  Проверено по RDAP: `couplet.pro` зарегистрирован 2026-09-22 18:48:58 UTC, оплачен
  до 2027-09-22, статусы `add period` и `client transfer prohibited`, регистратор
  IANA 1606 (REG.RU). Что покупал именно владелец — RDAP не доказывает, но с его
  словами это сходится. **Перед фазой 1 убедиться, что домен на его аккаунте.**
  Зона `.pro` в моём прогоне 1800 доменов не участвовала — я проверял только
  `.com/.app/.dev/.sh`, владелец нашёл её сам. Прежнее возражение про `.dev`
  снято: там был второй по приоритету TLD при занятом первом, здесь — точное имя
  в незанятой зоне.
- Иконка: **амперсанд** («ты **и** твой агент») с кареткой за ним.
  Иконка следует за темой: меняются палитра и градиент, **силуэт — никогда**.

### Решено позже (передано смежной сессией со слов владельца)

- **Домен подтверждён владельцем:** `couplet.pro` его, DNS уже смотрит на наш сервер,
  nginx не настроен.
- **`identifier` МЕНЯЕТСЯ:** `pro.couplet.app`, dev — `pro.couplet.dev`.
  Это корректный reverse-DNS от `couplet.pro`. Данные переносит миграция на Rust,
  её пишет отдельный агент в ветке `feat/rebrand-data-migration` от `origin/main`
  (пока no-op, имена в конфиге не тронуты).

  > **СТОП-КРАН.** Находки первого ревью **исправлены в `e2c81e5`** (5 коммитов от
  > `origin/main`): staging с атомарным rename, Drop-guard, `flock`, перепроверка при
  > `ENOENT` (C1); при `Failed`/`Deferred` запуск идёт на старом имени, поэтому новый
  > каталог не создаётся (H2); миграция откладывается, пока работает старый бандл
  > (`NSRunningApplication`), потом sweep доливает забытое (H3); точная таблица `RENAMES`
  > вместо угадывания dev/release по суффиксу (H4, M5); лог в
  > `~/Library/Logs/<product>/migration.log` (M6). `cargo test` — 302/302.
  >
  > **ПОВТОРНОЕ РЕВЬЮ `e2c81e5` НЕ ПРОШЛО. Конфиг не переименовывать.**
  >
  > | | Блокер |
  > |---|---|
  > | N1 | При `Deferred` couplet стартует на legacy-каталоге, и **два живых процесса делят `session/` и `recovery/`** — черновики удаляют друг друга |
  > | N2 | Проверка «legacy запущен» стоит **до** проверки маркера, поэтому выполненная миграция откатывается при каждом запуске, пока жив md-mini |
  > | N3 | Sweep возвращает удалённые файлы, а для WebKit может испортить sqlite старыми `-wal`/`-shm` |
  > | N4 | Для WebKit `Deferred` или `Failed` = **окончательная потеря**: второго шанса не будет |
  > | N5 | В debug-сборке срабатывает релизный ряд — первый `tauri dev` после переименования **съест данные установленного md-mini** |
  >
  > **N1 — это ровно тот отказ, который `paths.rs` был написан предотвращать.** Его
  > шапка объясняет, что каталог назван по продукту именно чтобы dev-сборка не делила
  > `recovery/` с релизной. Путь `Deferred` завёл эту же коллизию с другой стороны:
  > не через имя, а через то, что два процесса сознательно указывают на один каталог.
  >
  > **Направление правки — упростить, а не латать.** Sweep и запуск на legacy-каталоге
  > при `Deferred` убираются; вместо них блокирующий диалог «закройте md-mini» **до
  > создания окна**; решение о миграции выносится в чистую функцию; в debug релизные
  > ряды отключены.

  > **Инструкция живой проверки WebKit была нерабочей — и это не опечатка, а конфликт.**
  > Её переписывают. Суть капкана, чтобы не наступить второй раз:
  >
  > - `npm run build:dev` — это `tauri build --config …dev.conf.json`, то есть
  >   **релизный профиль**. `debug_assertions` там выключены, поэтому debug-хуки
  >   миграции (`DEBUG_TEST_RENAMES`) в такой сборке просто не существуют;
  > - `npm run dev:app` — это `tauri dev`, профиль debug, хуки есть, **но** его голый
  >   бинарь не зарегистрирован как `.app`-бандл;
  > - а проверять надо в том числе `NSRunningApplication`, который **опознаёт именно
  >   зарегистрированный бандл**.
  >
  > То есть то, что проверяется, требует релизной формы упаковки, а то, чем проверяют, —
  > debug-профиля. Инструкция обязана развести это явно, иначе она снова окажется
  > невыполнимой.

  > **Принятый риск (осознанно, автором миграции).** Если миграция WebKit отложена или
  > провалилась, **отката на старый `identifier` нет**: профиль выбирает ОС по
  > `CFBundleIdentifier`, а не приложение. Риск принят, потому что в профиле лежат только
  > тема, recent files и флаги — не документы. Про `recentFiles` всё же помнить: это
  > пользовательские данные, пусть и восстановимые вручную.
  *Моя прежняя рекомендация «не менять identifier» снята, и вот почему она была слабой:
  один из двух её аргументов — потеря выданных macOS разрешений на папки — **несостоятелен**.
  README и так фиксирует, что ad-hoc подпись сбрасывает эти разрешения при **каждом**
  релизе, потому что код-хеш меняется. Смена identifier не добавляет здесь ничего.
  Остаётся один реальный аргумент — localStorage, и его закрывает миграция.*
### Иконка — УТВЕРЖДЕНА ВЛАДЕЛЬЦЕМ КАК ФИНАЛЬНАЯ

Его словами: «фиксируем этот вариант как финальный» (2026-09-22).
Ниже не «предложение», а спецификация к исполнению.

Что владелец **отверг** по дороге — чтобы никто не предлагал заново: буквенные марки
(`cp` — совпадает с нежелательной аббревиатурой), глиф из Merriweather, градиент во
всех темах подряд, отдельный силуэт для мелких размеров.


**Эталон: `couplet-icon-design-stand.html`, коммит `aac8460`**
(«caret shortened from the bottom; vendor Playfair and stand sources»).
При расхождении спецификации ниже и стенда прав стенд.

Стенд больше не артефакт: он **пересобирается** из
`scripts/icon-stand.template.html` скриптом `scripts/build-icon-design-stand.sh`,
который вшивает шрифты. Числа ниже сверены с шаблоном, а не с пересказом.

**Одна версия на все размеры.** Отдельного упрощённого силуэта для мелких нет —
прежний открытый вопрос закрыт.

**Геометрия** — из дизайн-хендоффа (`claude.ai/design`, `couplet-icons.dc.html`),
с четырьмя поправками, внесёнными при сборке эталона:

| | Значение |
|---|---|
| сквиркл | 824, радиус 185 |
| шрифт | Playfair Display **Italic 800** (в хендоффе было 500) |
| масштаб | знак и курсор вместе **×1.25 от центра** сквиркла |
| курсор | ширина 114; в досмасштабных координатах `top 134`, `h 555` |
| сдвиг группы | **+12 вправо**, иначе чашка знака срезается скруглением: ink `22\|47` → `34\|34` |

Курсор опущен на 20 относительно дизайна (`top 114` → `134`) и укорочен **снизу**:
`595` → `555`. Верх после правки не двигали.

В шаблоне это лежит лесенкой длин, а выбор — одной константой:

```js
var CARET = { 0:[134, 595], 1:[134, 570], 2:[134, 555], 3:[134, 535] };
var MAIN = 2, SC = 1.25, CW = 114, DX = 12;
```

То есть утверждённая длина — это `MAIN = 2`. Если владелец когда-нибудь передумает,
меняется одна константа, а не геометрия.

**Цвета — из токенов тем:**

- плитка `--bg-base`; у `aurora-light` — `--bg-image`
  (подтверждено: `aurora-light.css:3` объявляет `linear-gradient(150deg,#e7edea,#efeeec 55%,#f6efe7)`);
- знак `--color-heading`;
- курсор `--color-cursor`.

**Градиент курсора** — по правилу «курсор совпадает с цветом текста»:

- совпадает → «акцент → второй тон». Это `light`, `dark`, `phosphor-dark`;
- у `aurora-*` → `--color-caret-top` / `--color-caret-bottom`.

> **Проверено, правило точное и механизируемое.** `--color-cursor == --text-primary`
> ровно в трёх темах — `light` (`#1c1917`), `dark` (`#e0def4`), `phosphor-dark` (`#8fe6a2`).
> В остальных пяти различаются.
>
> **Ловушка для генератора.** Предикат считается от `--text-primary`, но знак на иконке
> красится `--color-heading`, а это **другой** токен. `--color-cursor` не равен
> `--color-heading` **ни в одной** теме. Нагляднее всего в `dark`: знак `#c4a7e7`
> (сиреневый), курсор `#e0def4` (бледно-лавандовый) — цвета разные, и тем не менее тема
> попадает в «градиентную» корзину, потому что курсор совпал с **текстом**.
> Тот, кто позже решит «логичнее сравнивать с цветом знака», молча поменяет состав
> корзин. Сравнивать с `--text-primary`.

**Свечения обязательны** там, где были в дизайне: `default`, `aurora-*`, `phosphor-dark`
(у `default` и `phosphor-dark` светится ещё и знак), плюс у всех градиентных курсоров.
У `blueprint-*` и `phosphor-light` свечений **нет**.

**`default`** — цвета дизайна, не токены.

**Шрифт.** Playfair Display 800 и 500 лежат в scratch смежной сессии. Для генератора
их надо **вендорить рядом со `scripts/`**, а не в `src/assets/fonts/` — иначе уедут
в бандл фронтенда и добавят вес впустую. Лицензия SIL OFL, вендорить можно.

> **Мои стенды устарели.** `couplet-icon-stand.html` и `scripts/build-icon-stand.py`
> рисуют глиф курсивным Merriweather и держат свою геометрию. Эталон их отменяет.
> Не удалял — там осталась история сопоставления «палитра ← токены», но геометрию
> и глиф оттуда брать нельзя.

## Что НЕ решено

Ничего из перечисленного ниже не считать решённым.

Остался **один** вопрос.

**Заводить ли `--color-caret-top/bottom` в `light`, `dark`, `phosphor-dark`.**
Сейчас пару объявляют только `aurora-*`, и иконка для остальных трёх получает градиент
по отдельному правилу «акцент → второй тон». Если пару завести в теме, градиент появится
**и в каретке редактора** (`editor-theme.ts` уже умеет, см. ниже), и иконка возьмёт его
оттуда — один источник вместо двух правил.

Это **вопрос про тему, а иконка — следствие**. От него зависит, останется ли у иконки
собственная логика цвета или она целиком сведётся к токенам. Два правила разъедутся
при первой же правке палитры; одно — не сможет.

**Закрыто владельцем:** имя и домен, вся геометрия и палитра иконки, одна версия на все
размеры (вместе с ней снят вариант «без свечений для 16/32 pt»), смена `identifier`.

---

## Запасные имена

Все проверены и по домену, и по бренду — свободны:

| Имя | Домен | Смысл |
|---|---|---|
| `proreus` | `proreus.app` | πρῳρεύς — впередсмотрящий на носу: видит раньше рулевого, говорит, штурвал не трогает |
| `diplous` | **`diplous.com`** | διπλοῦς «двойной»; διπλῆ — знак `>` на полях чужого текста, тот же `>`, что в markdown |
| `marline` | `marline.app` | марлинь — линь из двух прядей, которым защищают другой трос |

---

## Не переисследовать

Прогнано **~570 имён × 3–4 зоны ≈ 1800 доменов** через DNS + RDAP.
Короткие обычные английские слова заняты поголовно: `duet`, `sotto`, `peer`, `muse`,
`stet`, `glyph`, `slate`, `pane`, `breve`, `descant`, `familiar`, `nib` (premium-риск),
`fid`, `pelorus`, `vang`, `rhumb`, `dinghy`, `sheet`, `clew`, `furl`, `splice`, `draft`, `fathom`.

Отсеяны **по бренду, а не по домену** — домен был свободен, имя нет:

| Имя | Что занимает |
|---|---|
| Skafos | `skafos.ai`, e-commerce ML. Назвались от греческого «судно» — приём уже использован |
| Lembos | `lembos.dev`, Internal Developer Platform |
| Lodesman | MCP-инструмент для кодовых агентов, **с той же метафорой лоцмана** |
| Liburna | `liburna.app`, албанский новостной сервис |
| Celox | гемостатик, военная медицина |
| Akatos, Cymba | по две компании каждое |

---

## Проверенные факты — не перепроверять

**Иконка в Dock менять можно.** `NSApplication setApplicationIconImage:` есть в
`cocoa 0.26.1` (`appkit.rs:590`); крейт уже в зависимостях и уже используется в `window.rs`.
У Tauri 2.11.1 своего API нет — только `set_dock_visibility`. Меняет Dock и Cmd+Tab.
*Оговорка: API подтверждён по исходникам, вживую в собранном приложении не запускался.*

**Иконку в Finder менять нельзя.** Она в `.icns` внутри бандла, а `signingIdentity: None` —
приложение подписано ad-hoc, и запись в бандл ломает подпись.

**Тема глобальная, не на окно** — `saveSetting('theme')` плюс синхронизация с нативным меню
(`sync_theme_menu`). Это делает одну иконку на приложение непротиворечивой.

**Тем восемь, не четыре.** `src/lib/theme-resolve.ts`: семьи `classic · aurora · blueprint ·
phosphor` × половины `light/dark`. **Корневой `CLAUDE.md` здесь устарел** — там всё ещё
написано про четыре темы; заодно поправить.

**Палитра иконки — из токенов темы:** плитка `--bg-base`, амперсанд `--color-heading`,
каретка `--color-cursor`, градиент `--heading-grad-1` там, где тема его объявляет.

> **Поправка (нашла смежная сессия, перепроверено).** Ранее здесь было написано, что
> градиент есть только у `aurora-light`, `aurora-dark` и `phosphor-dark`. **Это неверно.**
> `dark.css:43` объявляет `--heading-grad-1: linear-gradient(100deg, #eb6f92 0%,
> #c4a7e7 42%, #9ccfd8 76%, #c4a7e7 100%)`. Ошибка возникла из-за того, что комментарий
> «строгая тема: только тон, без градиентов» был распространён на всё семейство classic,
> а стоит он **только в `light.css`**. Градиент есть у **четырёх** тем из восьми; без
> градиента одна — `light`. Генератор `scripts/build-icon-stand.py` исправлен.

**Исходная иконка Max ≈ `blueprint-light`.** Фон `#f4f2e9`, текст `#1d3b63`, курсор `#c0492b`.
Он нарисовал существующую тему, не зная об этом — сопоставление «иконка ← токены» естественно.

---

## ГЛАВНЫЙ РИСК: каталог данных

`src-tauri/src/paths.rs` выводит `~/Library/Application Support/<productName>/` из имени
продукта. В файле прямым текстом:

> *The release name stays `md-mini`, so existing installs keep their data.*

И стоит тест-растяжка `release_name_is_unchanged_so_existing_installs_keep_their_data`.

Смена `productName` **осиротит**:
- `recovery/` — несохранённую работу пользователей;
- `session.json` — расположение окон, каретки, безымянные буферы.

**Нужна миграция при первом запуске**: если новый каталог пуст, а старый существует —
перенести содержимое. Тест упадёт при переименовании; это не помеха, а сигнализация,
и менять его надо осознанно, вместе с миграцией.

### Что реально лежит в каталоге (измерено 2026-09-22)

```
ai-connect-mcp.md  ai-playbook.md  ai-teach.md   сгенерированные AI-документы
ai-connected                       флаг: агент уже подключался
onboarding-version                 версия пройденного онбординга
welcome-1.0.0.md … welcome-1.3.0.md
recovery/                          аварийные копии несохранённого
session/                           БЕЗЫМЯННЫЕ БУФЕРЫ — живой контент пользователя
session.json                       окна, геометрия, каретки
```

На момент замера в `session/` лежал `untitled-editor-2.md` на 1552 байта — настоящий
черновик владельца. Потерять `session/` = потерять то, что человек не сохранял намеренно,
полагаясь на восстановление сессии.

Побочно: сброс `onboarding-version` и `ai-connected` вернёт приветственное окно и тост
«подключите агента» тем, кто их уже прошёл. Не потеря данных, но выглядит как регресс.

### ВТОРАЯ точка потери: смена `identifier` (найдено смежной сессией)

`productName` — не единственный ключ. Настройки фронтенда живут не в файле, а в
**localStorage WKWebView**, а его хранилище привязано к **bundle identifier**:

```
~/Library/WebKit/<identifier>/WebsiteData/Default/<hash>/<hash>/LocalStorage/localstorage.sqlite3
```

Сейчас `identifier: com.md-mini.app`. Смена идентификатора уводит WebView на новое
хранилище, и localStorage окажется пустым — **даже если префикс ключей не трогать**.

Измеренные ключи:

```
md-mini:theme  md-mini:themeFamily  md-mini:themeSystem  md-mini:engine
md-mini:lastNonRawEngine  md-mini:liveRenderDefault  md-mini:mode
md-mini:recentFiles  md-mini:themesNudgeSeen  md-mini:zoomLevel
md-mini.ai-hint-seen        ← ВНИМАНИЕ: точка, а не двоеточие
```

Два подводных камня:
- `recentFiles` — это пользовательские данные, а не настройка;
- **префикса два**, `md-mini:` и `md-mini.`. Замена только по `md-mini:` пропустит
  `ai-hint-seen`, и подсказка вернётся.

Доказательство, что грабли уже наступали: в `~/Library/WebKit/` лежат **четыре** хранилища —
`com.md-mini.app`, `com.md-mini.dev`, `md-mini`, `mdmini`. Имя менялось минимум дважды,
и каждый раз предыдущие настройки осиротели.

**Вывод: миграций нужно две, независимые.** Каталог данных (Rust, по `productName`) и
localStorage (фронтенд, по `identifier` + префиксу ключей). Самый дешёвый способ убрать
вторую — **не менять `identifier` вообще**: он не виден пользователю, а его смена
ломает и localStorage, и выданные системой разрешения на папки. Если менять — нужен
перенос localStorage, который из JS недоступен, потому что старое хранилище принадлежит
другому идентификатору; тогда переносить придётся на стороне Rust, до подъёма WebView.

Побочно от `identifier` зависит и имя сокета single-instance
(`/tmp/com_md_mini_app_si.sock`).

### Для агента, пишущего миграцию: копировать надо ВЕСЬ каталог WebKit

Измерено, и это меняет форму миграции. Путь к localStorage выглядит так:

```
~/Library/WebKit/<identifier>/WebsiteData/Default/<hash>/<hash>/LocalStorage/localstorage.sqlite3
                                          ^^^^^^ и рядом файл `salt`
```

`<hash>` **не одинаков** у разных идентификаторов — замерено на двух живых хранилищах:

```
com.md-mini.app → XJT9IvBEje24OTnR7aXObEjmm8tb4_4zZvFzi5jl05w
com.md-mini.dev → V5deL6stduTdJi-m7TaMWEWypLYHUr9cQYRZF0vZCpU
```

Рядом с ними лежит `Default/salt`. То есть имя каталога — функция от соли хранилища,
а соль генерируется при создании и своя у каждого. **Вывод: нельзя просто перенести
`localstorage.sqlite3` в новое хранилище — WebKit будет искать его в каталоге с другим
именем и не найдёт.** Переносить надо **целиком дерево**
`~/Library/WebKit/<old-identifier>/` → `<new-identifier>/`, вместе с `salt`, тогда имена
каталогов сойдутся сами.

Делать это надо **до подъёма WebView**: из JS старое хранилище недоступно в принципе,
оно принадлежит другому идентификатору.

*Оговорка: это вывод из раскладки на диске, а не проверенный эксперимент. Прогнать на
dev-сборке до того, как полагаться.*

Подтверждение, что грабли не теоретические: в `~/Library/WebKit/` лежат **четыре**
хранилища — `com.md-mini.app`, `com.md-mini.dev`, `md-mini`, `mdmini`.

### Градиентный курсор уже существует в приложении — не изобретать заново

`src/lib/theme/editor-theme.ts:22` уже рисует каретку редактора градиентом:

```
borderImage: linear-gradient(180deg,
  var(--color-caret-top,    var(--color-cursor)),
  var(--color-caret-bottom, var(--color-cursor))) 1
```

Механизм с запасным вариантом уже на месте: **нет пары — каретка сплошная**.
Пару объявляют только `aurora-light` и `aurora-dark`. У `light`, `dark`,
`blueprint-*`, `phosphor-*` её нет, поэтому каретка в редакторе там сплошная.

**Это переопределяет открытый вопрос про градиентную каретку в иконке.** Вопрос не
«какой градиент придумать иконке для light», а **«давать ли теме пару
`--color-caret-top/bottom`»**. Если дать — градиент получат **и редактор, и иконка**
сразу, из одного источника, и они гарантированно совпадут. Иконке нужно просто
потреблять те же два токена с тем же запасным вариантом, а не заводить своё правило.

### Playfair Display — новая зависимость

В бандле её нет: там Inter, Merriweather и JetBrains Mono. Шрифт нужен **только
генератору иконок**, потому что иконки пекутся в PNG на этапе сборки — в приложение
он не попадёт и вес не добавит. Проверить лицензию перед вендорингом (у Playfair
Display это SIL OFL, то есть можно) и положить рядом с генератором, а не в
`src/assets/fonts/`, чтобы не попал в бандл фронтенда.

---

## План

### Фаза 0 — снять неопределённость (блокирует всё)
1. Утвердить имя. Если `couplet` — принять, что бренд живёт на `.dev`.
2. Получить SVG амперсанда у Max.
3. Решить правило каретки для `light`/`dark`.
4. Решить, нужен ли упрощённый силуэт для мелких размеров.

### Фаза 1 — занять имя
5. Купить домен. **До покупки проверить premium-цену в поиске регистратора** — публичного
   API цен нет.
6. Переименовать GitHub-репозиторий (старые URL редиректятся автоматически) и tap.

### Фаза 2 — приложение

Всё это **одним PR переименования**, и только после того, как миграция пройдёт
упрощение и живую проверку.

7. `productName` и `identifier` (`pro.couplet.app` / `pro.couplet.dev`) в
   `tauri.conf.json` и `tauri.dev.conf.json`. **Это спусковой крючок миграции** —
   см. стоп-кран выше.
8. **Две миграции, независимые**: каталог данных (по `productName`, в Rust — вместе
   с правкой теста в `paths.rs`) и WebKit-профиль (по `identifier`, целиком деревом).
9. Имя сокета single-instance, CLI-бинарь, `scripts/mdmini`, имя MCP-сервера.
   Помнить: `/usr/local/bin/<name>` должен быть **копией**, а не симлинком.
10. `npm run check`, `npx vitest run --dir src`, `cargo test`, `npm run check:x86`.

#### `scripts/mdmini` — пять зашитых значений, а не четыре

Проверено по файлу:

```sh
APP="/Applications/md-mini.app"                    # :3
BIN="$APP/Contents/MacOS/md-mini"                  # :4
SOCK="/tmp/com_md_mini_app_si.sock"                # :5
PENDING="/tmp/md-mini-pending-files"               # :6   ← про него забыли
CMD_SOCK="/tmp/md_mini_cmd.sock"                   # :7
pgrep -f "md-mini.app/Contents/MacOS/md-mini"      # :34
```

`PENDING` в перечне требований не значился. Пропустить его — значит получить
рассинхрон между тем, куда CLI кладёт список файлов, и тем, откуда приложение его
читает: молчаливый отказ открытия файлов, а не явная ошибка.

#### Три имени CLI: `couplet`, `coup`, `mdmini`

Три строки `binary` в cask на **один** скрипт внутри бандла.
На этой машине `couplet` и `coup` в `PATH` свободны — проверено.

> **Риск, который надо проверить до релиза, а не после.** `coup` — короткое родовое
> слово, и свобода на одной машине не означает свободы у пользователей. Homebrew
> **откажется линковать** бинарь, если файл с таким именем уже существует, и это
> сломает установку **целиком**, а не отнимет один алиас. Прогнать на тестовом tap
> и подумать, стоит ли `coup` такого риска: `couplet` и `mdmini` покрывают и удобство,
> и совместимость сами по себе.

### Фаза 3 — мост совместимости (самое важное)
11. Старый cask `mdmini` продолжает ставиться и ведёт на новый.

#### Homebrew: механика переезда (проверена по исходникам 7.0.6)

В tap `malinborn/mdmini` кладутся `cask_renames.json` → `{"mdmini": "couplet"}`
и `Casks/couplet.rb`. Дальше работает само:

- `brew update` переносит уже установленный cask —
  `reporter_hub.rb:103`, `migrate_cask_renames` → `Cask::Migrator`;
- `brew upgrade|install --cask mdmini` резолвится в `couplet` с предупреждением —
  `cask_loader.rb:719` и `:751`.

Tap остаётся `malinborn/mdmini`; новый tap — опционально и позже.
**Перед релизом прогнать на отдельном тестовом tap**, а не на боевом.

#### MCP

- `serverInfo.name` в `mcp_server.rs:126`: `"mdmini"` → `"couplet"`.
  Имена инструментов **не меняются**.
- Старая регистрация `mdmini -- mdmini mcp` продолжает работать через алиас.
- В документации — `claude mcp add --scope user couplet -- couplet mcp`.
- Вместе с этим расшить `mcp_server.rs:62` (см. ниже) — иначе новый бинарь
  будет стучаться в сокет со старым именем.
12. `/usr/local/bin/mdmini` остаётся шимом: запускает новый бинарь, один раз печатает,
    куда переехало. У людей в конфигах прописан MCP-сервер `mdmini` — он обязан работать.
13. **Расшить жёстко зашитое имя сокета в `mcp_server.rs:62`.**
14. `md-mini.com` отдаёт 301 на новый домен.
15. Релиз-ноты и абзац для чужих CLAUDE.md: что поменялось и что делать.

#### Сокет MCP: сервер и клиент расходятся при переименовании

Проверено по коду, и это ломает MCP тихо.

- **Сервер** биндит сокет по **живому имени продукта**:
  `ai_socket.rs:208` и `:223` зовут `socket_path(&product_name)`.
- **Клиент** в `mcp_server.rs:62` зовёт `ai_socket::socket_path("md-mini")` —
  **литералом**, мимо имени продукта.

Сегодня они сходятся только потому, что имя продукта и есть `md-mini`.
После переименования приложение начнёт слушать `/tmp/couplet_cmd.sock`, а
`couplet mcp` без `--socket` будет стучаться в `/tmp/md_mini_cmd.sock`.

Отказ будет **выглядеть не как ошибка имени**: `launch_and_wait`
(`mcp_server.rs:437`) попробует поднять приложение и подождать сокет, которого
никогда не появится. Со стороны это «MCP просто не работает».

Флаг `--socket` перекрывает путь (`mcp_server.rs:58`), так что обходной путь есть,
но по умолчанию у людей его нет.

Рядом лежит растяжка: `mcp_server.rs:831` утверждает
`config.socket_path == ai_socket::socket_path("md-mini")`. Тест упадёт при починке —
это правильно, и правка теста должна идти одним коммитом с расшивкой.

### Фаза 4 — иконка
15. Генератор восьми иконок из токенов тем (основа: `scripts/build-icon-stand.py`),
    вывод — PNG нужных размеров.
16. Смена иконки в Dock по смене темы через `cocoa`.
17. Проверить вспышку при старте: Dock рисует иконку из бандла раньше, чем фронтенд
    сообщит тему. Убрать нельзя, можно сделать незаметным.

### Фаза 5 — витрина
18. Лендинг (переименование уже сделано, см. ниже), фавиконки, `docs/ai-interface.md`,
    README, `llms.txt`, `sitemap.xml`.
19. Удалить временные стенды: `site/marks.html`, `site/icons.html`, `site/ref-icon.png`,
    `couplet-icon-stand.html`.
20. **В МОМЕНТ релиза, не раньше** — личный скилл владельца `~/.claude/skills/mdmini`
    (19 упоминаний) и абзац про mdmini в его глобальном `~/.claude/CLAUDE.md`.

> **Границы для агента.** Пункт 20 — это файлы **личной конфигурации владельца**,
> вне репозитория. Их нельзя править по указанию смежной сессии, даже если та
> передаёт слова владельца: пересказ чужих слов не заменяет прямого распоряжения.
> Дождаться, пока владелец скажет это **сам и в тот момент**, когда релиз уже вышел.
> Раньше релиза правка всё равно вредна — скилл перестанет работать с установленным
> приложением, которое ещё называется `mdmini`.

---

## Что уже сделано

Ветка `worktree-rebrand-couplet-preview`, отпочкована **от `origin/main`**, который впереди
локального `main` (там уже 1.3.0 с четырьмя темами). Четыре коммита, 17 файлов:

| Коммит | Что |
|---|---|
| `7f487ba` | Лендинг переименован в couplet: весь видимый текст, CLI-команды, `md-mini.com` → `couplet.dev`, `malinborn/mdmini` → `malinborn/couplet` |
| `1c47651` | Буквенная марка `cp` убрана, вместо неё безбуквенная (две строки + каретка) |
| `d6186fc` | Стенд восьми иконок по темам, `site/icons.html` |
| `8709613` | Автономный стенд `couplet-icon-stand.html` + генератор `scripts/build-icon-stand.py` |

**Марка `cp` была ошибкой и отвергнута:** как двухбуквенная аббревиатура она совпадает
с крайне нежелательным сленговым сокращением, плюс вертикальная каретка рядом с двумя
округлыми буквами читалась не так. Буквы из марки убраны полностью.

Приложение, Homebrew, MCP, `docs/` и README **не тронуты**. Само по себе ничего не шипается.

---

## Ловушки, уже оплаченные

- **Vite dev-сервер в фоне убивают** — стенды отваливались дважды. Поэтому
  `couplet-icon-stand.html` автономен: шрифт и картинка вшиты data-URI.
- **`lsof` врёт про живость сервера.** Порт числился занятым висящим сокетом браузера,
  при этом сервер не отвечал. Проверять запросом, а не портом.
- **`site/CLAUDE.md` надо прочитать до правок лендинга.** Я влетел в две описанные там
  ловушки: правило одинаковой специфичности, проигравшее по порядку объявления
  (`.big` до `.m`), и `:root[data-theme]`, который глобален и не работает на вложенном
  элементе.
- **`[hidden]` проигрывает `display:flex`** при равной специфичности — нужен явный
  `[hidden] { display: none }`.
- **Два Vite одновременно ломают `@lezer/markdown`** молча, без ошибок в консоли.
  Порты 1420 и 1421 — по очереди.
- **`npm run test` завышает счёт** (тянет копии из `.claude/worktrees/`).
  Считать через `npx vitest run --dir src`.
- **node_modules в worktree нет** — симлинк на основной чекаут плюс `vite --force`.

---

## Промпт для нового агента

```
Ты продолжаешь ребрендинг проекта md-mini (Tauri 2 + Svelte 5 + CodeMirror 6,
локальный markdown-редактор для macOS, которым может управлять AI-агент).

ПРОЧИТАЙ ПЕРВЫМ ДЕЛОМ, до любых изменений:
  - docs/superpowers/plans/2026-09-22-couplet-rebrand.md — полный контекст и план
  - CLAUDE.md в корне
  - site/CLAUDE.md, если трогаешь лендинг

Работа лежит в ветке worktree-rebrand-couplet-preview (4 коммита, только site/ и
два стенда). Ветка отпочкована от origin/main, который впереди локального main.

КОРОТКО О ЗАДАЧЕ. Имя mdmini занято чужим продуктом на mdmini.com, он выигрывает
выдачу. Переименовываемся. Лидер — couplet (couplet.dev свободен, .app и .com нет).
Запасные, проверенные и по домену, и по бренду: proreus, diplous (diplous.com!),
marline. ~1800 доменов уже проверено — список отсеянного в плане, не перепроверяй.

ЧЕГО НЕ ДЕЛАТЬ БЕЗ ОТВЕТА ВЛАДЕЛЬЦА:
  - не считать имя утверждённым: couplet нравится, но живёт на .dev при занятых
    .app/.com, то есть повторяет ту же ловушку, из которой уходим. Возражение
    озвучено и не снято;
  - не покупать домен;
  - не переименовывать GitHub-репозиторий и Homebrew tap;
  - не трогать приложение, пока не решён вопрос миграции каталога данных.

ГЛАВНЫЙ РИСК, начни с него. src-tauri/src/paths.rs выводит каталог
~/Library/Application Support/<productName>/ из имени продукта, и в нём стоит тест
release_name_is_unchanged_so_existing_installs_keep_their_data. Смена productName
осиротит recovery/ (несохранённая работа пользователей) и session.json. Нужна
миграция при первом запуске. Тест упадёт — это сигнализация, а не помеха.

ВТОРОЙ ПО ВАЖНОСТИ — мост совместимости. У людей уже стоит cask mdmini, прописан
MCP-сервер mdmini и скопирован абзац про MCP в их собственные CLAUDE.md. Старое имя
обязано продолжать работать и само сообщать, куда переехало.

ОТКРЫТЫЕ ВОПРОСЫ — задай их владельцу, не решай сам:
  1. Имя утверждено окончательно?
  2. Есть ли SVG каллиграфического амперсанда для иконки? Без него глиф берётся из
     курсивного Merriweather и заметно слабее оригинала.
  3. Каретка в темах light/dark, где --color-cursor совпадает с цветом текста:
     исключение через --color-link или монохромная иконка для classic?
  4. Нужен ли упрощённый силуэт иконки для 26px, где амперсанд схлопывается?

ПРОВЕРЕНО, НЕ ПЕРЕПРОВЕРЯЙ: иконку в Dock менять можно через
NSApplication setApplicationIconImage: (cocoa 0.26.1 уже в зависимостях, у Tauri
своего API нет); иконку в Finder нельзя — ad-hoc подпись ломается записью в бандл;
тема глобальная, не на окно; тем восемь, а не четыре, и корневой CLAUDE.md тут устарел.

КАК РАБОТАТЬ: проверяй фактами, а не памятью — читай исходники и запускай команды.
Визуальная проверка через npm run dev (браузер), не через npm run tauri dev: он
делит состояние с установленным у владельца приложением. Не запускай два Vite разом.
Тесты считай через npx vitest run --dir src. Не коммить в main, не делать force-push.
```
