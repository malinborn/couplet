# Заметки архитектора для реализатора вкладок

Запасной канал на случай, если сообщения между сессиями застрянут на подтверждении. Сюда дублирую ответы и ревью. Проверяй через `git fetch && git show origin/worktree-tabs-design-research:docs/superpowers/plans/tabs-architect-notes.md`. Новые записи сверху.

## 2026-09-25 — ревью плана 05 (`2026-09-25-tabs-05-carousel.md`, 102b152)
- **Одобрен, исполнять.** D1–D13 приняты. D3 делает flush с повторами до отказа. ⌘M работает только при открытом дровере, есть тест-сторож против предопределённого Minimize в `menu.rs`.
- В Q11 для Max добавить: вне дровера ⌘M ничего не делает, а пользователи macOS могут ждать Minimize.

## 2026-09-25 — ревью плана 04 (`2026-09-25-tabs-04-ai-cli.md`, 3d952ec + 1d59ed0)
- **Одобрен, исполнять.** D1–D19 приняты. D2 — уже открытый файл побеждает живой binding — прямое следствие правила «один файл — одна вкладка».
- **Ответы на вопросы:**
  1. Ошибка мёртвого номера — на английском, раскладка по спеке.
  2. Q6 снят: `focus: true` по умолчанию согласован с Max при дизайне.
  3. Q7 оставить в вопросах с дефолтом «фоновое окно без активации».
  4. Q8 — ок.
  5. Асимметрия CLI и Finder — ок до ответа на Q4.
  6. TransientBar — первый набросок, скриншоты во всех 4 темах, в отчёте пометка «нужен глаз Max».
  7. План 05 обязан переносить входящие агента и AiPending вместе с вкладкой.

## 2026-09-25 — план 03 принят (PR #95 = 02+03), что должно попасть в план 04
- Сверил скриншоты с макетом (dark-open, aurora-light-closed, dev-02-open, dev-05-unviewed): совпадает. D9 (поиск по кешу вместо диска) — follow-up в Known gaps.
- **План 04 обязан покрыть:**
  - **§3:** ошибка мёртвого номера со списком окон; `coup ls` (с `--json`); `coup close`.
  - **§4:** человек и агент различаются по `CLAUDECODE`. Флаги `-t`, `-b`, `-f` работают в любой команде открытия. Человеческий `coup file` без `-t` открывает новое окно проекта.
  - **§5:** маршрутизация в четыре шага. Проект — корень репозитория, у worktree свой (`git_info.rs`). `show` возвращает `{ok, window, focused}`, `focus` по умолчанию true. Новые MCP-команды `close` и `windows`. `edit` в фоновую вкладку идёт в кешированный state.
  - **§7:** `transient` — локальная карточка. «Проигнорировано» значит просмотрено и без ответа в течение часа. Непросмотренная вкладка сама не закрывается. Радиопункты в меню File.
  - **§8:** ИИ и таймаут никогда не закрывают untitled с текстом.
  - **Доки и контракт:** описание `show`, `MCP_AGENT_SNIPPET`, `agent_text`, `ai-interface.md`. Скилл и глобальный CLAUDE.md Max не трогать, дифф для них положить в `tabs-questions.md`.
  - **Тесты CLI и MCP** — только с `--socket /tmp/md_mini_dev_cmd.sock`.
  - **Q4 (Finder):** до ответа Max оставить текущее поведение.
  - **§13.3, дефолт:** таймаут `ask` считается от запроса; вопрос доставляется при активации вкладки, если не истёк.

## 2026-09-25 — ревью плана 03 (`2026-09-25-tabs-03-drawer.md`, b7fc480)
- **Одобрен, исполнять.** Принятые решения:
  - «просмотрено» — вкладка активна, пока окно в фокусе;
  - перестановка через `sync`;
  - ⌘L/⌘R/⌘U — клавиши вебвью только при открытом дровере, с тестом-сторожем; ⌘J — пункт меню;
  - группа: закрыть и «В новые окна»; перенос в другое окно — план 05;
  - Esc: запрос → выделение → закрыть;
  - закрытие по уходу курсора только у дровера, открытого наведением;
  - ⌘1…9 при фильтре идут по видимому порядку.
- **Перехват набора:** берём дефолт реализатора. Дровер, открытый наведением, захватывает печать, только если последний ввод был не в редакторе; открытый ⌘J — сразу. Это UX-отступление, в `tabs-questions.md` как Q5 для Max.

## 2026-09-25 — план 02 принят (PR #95), дальше 03
- В плане 04 должна быть явная задача: агентские открытия по умолчанию идут в фон (`focus: false`, спека §5), а активная вкладка не угоняется, пока человек в ней пишет. Так закрывается проблема «после D7-активации ввод человека попадает в файл агента».
- План 03: эталон — `docs/investigations/2026-09-24-tabs-mockup/drawer.html` на теге `tabs-mockup-approved`, в визуале прав макет. Хлястик 150px на 25.8px, весь текст вертикальный, номер окна акцентом темы, ширина равна кнопке ИИ. Шапка на одной базовой линии, подсказка про ⇧ появляется только при зажатом ⇧. Фильтр набором с приоритетом имени, клавиши ⌘J/⌘L/⌘R/⌘U, режим Compact. В плане нужно скриншотное сравнение с макетом в Playwright по всем четырём темам.

## 2026-09-25 — ревью плана 02 (`2026-09-25-tabs-02-model.md`, e4d582f)
- **Одобрен, исполнять.** D5, D9, D10, D11–D17 и правка CLAUDE.md в задаче 15 приняты.
- **Уточнение D4/D7.** «Отказ, если не сохранено» означает: сначала flush автосейва, отказ только при реально упавшем сохранении (save-error / unsaved-blocked) или, для D7, при живом ask другого агента. Нужен тест в controller: переключение сразу после набора — flush и успех, без отказа.
- **Уточнение D6.** Отмена ask при уходе вкладки в фон — только временно, в одном релизе с 04. В Known gaps записать явную передачу в план 04: ask переживает уход вкладки в фон (вкладка переливается, вопрос доставляется при активации, агент не получает ошибку, спека §5).

## 2026-09-24 — замечание по 7d9310d
- Коммит `chore(agents): run md-mini-dev on opus` (sonnet → opus в `.claude/agents/md-mini-dev.md`). По словам реализатора, это прямая просьба Max этой ночью. Вопрос снят, в `tabs-questions.md` не нужен, достаточно пометки в ночном отчёте: коммит в PR фундамента, его можно вынести cherry-pick'ом.

## 2026-09-24 — план 01 готов
- `docs/superpowers/plans/2026-09-24-tabs-01-foundation.md`, коммит `fa8e11c`. Финальный, можно исполнять.
- Моё ревью уже внесено:
  - в задаче 4 закрывается hover-меню (`hideHoverMenu` в `hover-menu.ts`), а не только правка ячейки;
  - в ручной проверке №6 сценарий коллизии идёт через пустое окно `main`: ⌘T в фундаменте ещё нет.
- Одно имя из спеки план поменял сам: черновик untitled именуется «process-unique id» (`tab_id` в `WindowSnapshot`), а не uuid. По смыслу это одно и то же, оставляем как в плане.
