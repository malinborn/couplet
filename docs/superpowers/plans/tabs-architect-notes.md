# Заметки архитектора для реализатора вкладок

Запасной канал на случай, если сообщения между сессиями застрянут на подтверждении. Сюда дублирую ответы и ревью. Проверяй через `git fetch && git show origin/worktree-tabs-design-research:docs/superpowers/plans/tabs-architect-notes.md`. Новые записи сверху.

## 2026-09-25 — план 02 принят (PR #95), дальше 03
- В плане 04 должна быть явная задача: агентские открытия по умолчанию идут в фон (`focus: false`, спека §5), а активная вкладка не угоняется, пока человек в ней пишет. Так закрывается проблема «после D7-активации ввод человека попадает в файл агента».
- План 03: эталон — `docs/investigations/2026-09-24-tabs-mockup/drawer.html` на теге `tabs-mockup-approved`, в визуале прав макет. Хлястик 150px на 25.8px, весь текст вертикальный, номер окна акцентом темы, ширина равна кнопке ИИ. Шапка на одной базовой линии, подсказка про ⇧ появляется только при зажатом ⇧. Фильтр набором с приоритетом имени, клавиши ⌘J/⌘L/⌘R/⌘U, режим Compact. В плане нужно скриншотное сравнение с макетом в Playwright по всем четырём темам.

## 2026-09-25 — ревью плана 02 (`2026-09-25-tabs-02-model.md`, e4d582f)
- **Одобрен, исполнять.** D5, D9, D10, D11–D17 и правка CLAUDE.md в задаче 15 приняты.
- **Уточнение D4/D7.** «Отказ, если не сохранено» означает: сначала flush автосейва, отказ только при реально упавшем сохранении (save-error / unsaved-blocked) или, для D7, при живом ask другого агента. Нужен тест в controller: переключение сразу после набора — flush и успех, без отказа.
- **Уточнение D6.** Отмена ask при уходе вкладки в фон — только временно, в одном релизе с 04. В Known gaps записать явную передачу в план 04: ask переживает уход вкладки в фон (вкладка переливается, вопрос доставляется при активации, агент не получает ошибку, спека §5).

## 2026-09-24 — замечание по 7d9310d
- Коммит `chore(agents): run md-mini-dev on opus` (sonnet → opus в `.claude/agents/md-mini-dev.md`). По словам реализатора, это прямая просьба Max этой ночью. Вопрос снят, в `tabs-questions.md` не нужен, достаточно пометки в ночном отчёте: коммит в PR фундамента, его можно вынести cherry-pick'ом.

## 2026-09-24 — план 01 готов
- `docs/superpowers/plans/2026-09-24-tabs-01-foundation.md`, коммит `fa8e11c`. Финальный, можно исполнять.
- Моё ревью уже внесено:
  - в задаче 4 закрывается hover-меню (`hideHoverMenu` в `hover-menu.ts`), а не только правка ячейки;
  - в ручной проверке №6 сценарий коллизии идёт через пустое окно `main`: ⌘T в фундаменте ещё нет.
- Одно имя из спеки план поменял сам: черновик untitled именуется «process-unique id» (`tab_id` в `WindowSnapshot`), а не uuid. По смыслу это одно и то же, оставляем как в плане.
