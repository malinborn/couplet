# Вкладки — ночной отчёт

*Ночь 2026-09-24 → 25 · сессия «couplet tabs implementation» · worktree `.claude/worktrees/tabs-impl` · вопросы на утро: [`tabs-questions.md`](tabs-questions.md)*

> **Статус на момент записи (≈06:50):** план 01 закончен, draft PR открыт. План 02 написан, одобрен архитектором, выполнено 10 задач из 15, идёт одиннадцатая. В приложении вкладок пока не видно: всё до задачи 12 — фундамент под контроллер, поведение окон не менялось. Планы 03–05 не начаты. Этот отчёт обновляется по ходу ночи; последняя версия — в ветке `feat/tabs`.

## Коротко

| План | Ветка | Состояние | PR |
|---|---|---|---|
| 01 Фундамент | `feat/tabs-foundation` | **готов**: 8 задач, ревью, живой QA 9/10 | [#94](https://github.com/malinborn/couplet/pull/94) (draft) |
| 02 Модель вкладок | `feat/tabs` | в работе: задачи 1–4 из 15 готовы | будет один draft PR `feat/tabs` → main |
| 03 Хлястик и дровер | — | не начат | — |
| 04 Контракт ИИ и CLI | — | не начат | — |
| 05 Drag между окнами | — | не начат | — |

**Найденный риск для данных (отдельной строкой, по просьбе архитектора):** `paths::app_data_dir()` до вызова `paths::init` **молча возвращает каталог релизного приложения** (`md-mini`), а не dev. Любое состояние, созданное до `init` в dev-сборке, читает и пишет данные установленного couplet. В плане 01 Recent Files из-за этого регистрируется только внутри `setup` после `init`, а `RecentFiles::default()` удалён, чтобы его нельзя было случайно поставить на builder. Сам fallback в `paths.rs` не менял — стоит решить, должен ли он паниковать.

## Тесты

| | Старт ночи | После плана 01 | Сейчас (`feat/tabs`) |
|---|---|---|---|
| vitest (`--dir src`) | 1314 / 57 файлов | 1353 / 61 | 1452 / 67 |
| cargo test | 308 passed, 3 ignored | 368 / 3 | 419 / 3 |
| clippy | 51 предупреждение | 51 (новых нет) | 51 |
| `npm run check` | 0 ошибок | 0 | 0 |
| `npm run check:x86` | — | собирается | — |

51 предупреждение clippy было до начала работы (план ждал «без предупреждений»); ни одна задача не добавила новых.

## План 01 — фундамент (PR #94)

Что сделано (спека §8–§9):
- **Утечка undo** при ⌘O / Recent закрыта: пересборка history-compartment. ⌘Z больше не вписывает текст старого документа в новый.
- **Потеря последних ≤300 мс набора** при переключении закрыта: автосейв сбрасывается до замены.
- **`switchDocument`** — единственный путь замены документа: дедуп между окнами, несохранённый буфер не теряется, передача пауз и черновиков комментариев, `ask`/`edit`/`show` уходящего документа сразу получают ошибку, переключения идут очередью, откат при отмене, открытая ячейка таблицы сохраняется.
- **Черновики untitled** именуются по `tab_id`, а не по label окна (коллизия `untitled-main.md` между запусками).
- **Recent Files в Rust** (`recent.json`), рассылка с версиями во все окна, разовый импорт из localStorage.
- **⌘⇧T** возвращает последнее закрытое окно с курсором; в свежем запуске — сессию. В тосте сессии кнопка «Restore».

Живой QA в dev-приложении (MCP-мост, `osascript` по PID, боевое приложение не трогали): 9 сценариев PASS, «Сохранить как» не проверить (NSSavePanel без нажатий клавиш не увести). Скриншоты:
[foundation-s4-untitled-kept](night-shots/foundation-s4-untitled-kept.png) ·
[foundation-s4-file-in-new-window](night-shots/foundation-s4-file-in-new-window.png) ·
[foundation-s5-ask-before-switch](night-shots/foundation-s5-ask-before-switch.png) ·
[foundation-s6-restore-toast](night-shots/foundation-s6-restore-toast.png) ·
[foundation-s6-untitled-restored](night-shots/foundation-s6-untitled-restored.png) ·
[foundation-s8-reopened-closed](night-shots/foundation-s8-reopened-closed.png)

### Отступления от плана 01 и почему
1. **Тест утечки undo в плане ничего не воспроизводил** — в нём была правка-вставка, а её инверсия схлопывается при полной замене документа. Утечку дают правки-удаления (так и было в прожарке №3). Тест переписан.
2. **Правило «отказ, если файл после сохранения всё ещё несохранён»** + повторное сохранение прямо перед заменой + флаг на время переключения (иначе `ask` старого документа мог проскочить и повиснуть). По итогам ревью.
3. **Закрыты две гонки потери восстановленных черновиков untitled** (вне текста плана, в его цели): первое сообщение окна в сессию уходило до загрузки текста и стирало ссылку на черновик (потеря даже при обычном ⌘Q в первые ~5 с); уборка удаляла черновики ещё не открытых окон во время восстановления.
4. **Recent регистрируется после `paths::init`** (см. риск выше), рассылки с версиями против устаревших событий.
5. **⌘⇧T**: после первого ⌘W сессию возвращает только кнопка в тосте — см. вопрос Q1.
6. **Попутно исправлено**: «Сохранить как» не регистрировал новый путь (не работали dedup и слежение за файлом); событие `open-file` уходило во все окна; открытие из Finder перетирало чужую запись и файл оказывался в двух окнах.
7. **Коммит `7d9310d`** (агент `md-mini-dev` → Opus) — по твоей просьбе этой ночью; лежит в PR фундамента, можно вынести cherry-pick'ом.
8. **PR #94 несёт всю дизайн-документацию** (~4.8k строк: спека, исследования, макет) — реши, оставлять в нём или выносить.

### Что из плана 01 перенесено в план 02
- Действия нативного меню доставлялись в каждое окно N раз (было и в `main`) — **сделано в плане 02, задача 1**.
- Пошаговая логика переключения не покрыта автотестами — выносится в модуль с DI и тестами (план 02, задача 10).
- `AiPending` — единая запись по (окно, путь) (план 02, задача 7).
- ⌘W пока закрывает окно из Rust без сброса последних ~300 мс (план 02, задачи 12–13).

### Известное, но не исправленное
- Пять старых мест «файл уже открыт → фокус» не разворачивают свёрнутое окно (исправлено только в новом `focus_if_open`).
- Восстановление сессии не переиспользует пустое окно main (было и раньше).
- Через ~2 с после перезапуска `session.json` перезаписывается новой сессией, прошлая остаётся только в памяти до Restore — при жёстком убийстве до Restore она теряется (было и раньше, по замыслу).
- Самый первый черновик нового untitled-окна уборка может удалить сразу после записи; через 5 с он пишется снова.

## План 02 — модель вкладок (ветка `feat/tabs`)

План: [`2026-09-25-tabs-02-model.md`](2026-09-25-tabs-02-model.md), 15 задач, написан агентом по скиллу writing-plans, одобрен архитектором с двумя уточнениями (отказ переключаться — только если сохранение реально не прошло, с тестом «переключение сразу после набора»; `ask` в фоновой вкладке — явная передача в план 04).

| # | Задача | Коммиты | Итог |
|---|---|---|---|
| 1 | Действия меню — ровно в одно окно | `7572040`, `1d60e4d` | готово; ревью: не выбирать свёрнутое/скрытое окно — исправлено |
| 2 | Свой тост «ещё не на диске» | `ef0fd3e` | готово |
| 3 | Запись комментариев одной очередью | `db9c35c`, `ef41652`, `f6482cb` | готово; попутно закрыты 2 старые гонки, одна теряла набранный текст |
| 4 | Реестр вкладок за `OpenFiles` | `5e9be56`, `f43f984` | готово |
| 5 | Session v2 (`session-v2.json`, миграция v1, `session.json` не пишется) | `fa85948`, `9d96586` | готово; ревью: сессия выбирается по времени сохранения (иначе откат dev-сборки терял черновики), фоновые вкладки не выпадают из сессии, ссылка на черновик пишется раньше файла |
| 6 | Номера окон `#N`, заголовок `README.md — #7` | `0c850a7`, `014e256` | готово; найдено: CLI может открыть окно до `setup` — `state()` уронил бы приём CLI до перезапуска |
| 7 | `AiPending` одной записью по (окно, путь), очередь чистится | `b69aec5` | готово |
| 8 | Чистая модель вкладок и решения «уйти / открыть» | `26f7958` | готово |
| 9 | Одна фабрика состояний, подмена состояния целиком | `0d1ed2e`, `3f8bb62` | готово; ревью поймало регрессию прокрутки (новый документ открывался на прокрутке старого), проверено в Chrome |
| 10 | Контроллер вкладок (DI, 42 теста) | `bf8221b`, `198e99a` | готово; ревью: текст удалённого в фоне файла терялся; упавшее открытие оставляло файл «занятым» до перезапуска — исправлено |
| 11 | Rust-команды вкладок, стек закрытых вкладок, ⌘⇧T | — | в работе |
| 12–15 | Проводка в App.svelte, клавиши, временный список, проверка | — | впереди |

### Отступления в процессе плана 02
- Проверка на соответствие плану и ревью качества идут **одним ревьюером в два этапа** на задачу (в плане 01 были два отдельных агента) — ради времени.
- Исполнитель получает свою задачу **диапазоном строк плана**, а не пересказом: задачи до 1.7k строк.
- Ручная проверка в dev-приложении для задач 1–14 отложена до итоговой задачи 15 (одна сессия с приложением вместо пятнадцати).

## Что осталось
- План 02: задачи 5–15, итоговое ревью, draft PR `feat/tabs`.
- Планы 03, 04, 05: детальные планы ещё не написаны.
- Перед планом 05: ручной тест pointer-событий за краем окна (мыши у меня нет) — будет в вопросах на утро.

## Вопросы на утро
[`tabs-questions.md`](tabs-questions.md)
